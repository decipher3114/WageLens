export enum MicCardMode {
  Idle = "idle",
  Listening = "listening",
  Speaking = "speaking",
}

export enum MicPulseVariant {
  Idle = "idle",
  Listening = "listening",
  Feedback = "feedback",
}

export enum ComplaintStatus {
  Accepted = "accepted",
  Discarded = "discarded",
}

export enum ExtractionField {
  Platform = "platform",
  TripTime = "trip_time",
  PickupLocation = "pickup_location",
  DropLocation = "drop_location",
  QuotedAmount = "quoted_amount",
  PaidAmount = "paid_amount",
}
